<Page x:Class="PingTrack.View.Pages.PlayersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PingTrack.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
       Title="Игроки"
      Background="{StaticResource WindowBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="5"
                   Text="Игроки секции"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>

        <Border Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"
                Style="{StaticResource Card}">
            <DataGrid x:Name="PlayersDataGrid"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Возраст" Binding="{Binding Age}" Width="120"/>
                    <DataGridTextColumn Header="Группа" Binding="{Binding Group}" Width="200"/>
                    <DataGridTextColumn Header="Контактный номер" Binding="{Binding Phone}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Button x:Name="AddPlayerButton"
                Grid.Row="2" Grid.Column="2"
                Content="Добавить игрока"
                Style="{StaticResource PrimaryButton}"
                Width="160"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>

        <Button x:Name="DeletePlayerButton"
                Grid.Row="2" Grid.Column="3"
                Content="Удалить"
                Style="{StaticResource BaseButton}"
                Width="120"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
    </Grid>
</Page>