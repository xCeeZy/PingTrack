<Window x:Class="PingTrack.View.Windows.AddEditGroupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PingTrack.View.Windows"
        mc:Ignorable="d"
        Title="Добавление группы"
        WindowStartupLocation="CenterScreen"
        Width="440" Height="400"
        Background="{StaticResource WindowBrush}"
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="1"
                   Text="Добавление группы"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Название группы:" FontSize="14"
                       Foreground="{StaticResource TextBrush}"
                       VerticalAlignment="Bottom"/>
            <TextBox x:Name="GroupNameBox"
                     Grid.Row="1"
                     FontSize="14"
                     VerticalContentAlignment="Center"
                     Height="32"/>
        </Grid>

        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Тренер:" FontSize="14"
                       Foreground="{StaticResource TextBrush}"
                       VerticalAlignment="Bottom"/>
            <ComboBox x:Name="CoachComboBox"
                      Grid.Row="1"
                      FontSize="14"
                      Height="32"
                      DisplayMemberPath="Login"
                      SelectedValuePath="ID_User"/>
        </Grid>

        <Grid Grid.Row="3" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Уровень подготовки:" FontSize="14"
                       Foreground="{StaticResource TextBrush}"
                       VerticalAlignment="Bottom"/>
            <ComboBox x:Name="LevelComboBox"
                      Grid.Row="1"
                      FontSize="14"
                      Height="32"
                      DisplayMemberPath="Level_Name"
                      SelectedValuePath="ID_Level"/>
        </Grid>

        <Grid Grid.Row="4" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="SaveButton"
                    Content="Сохранить"
                    Style="{StaticResource PrimaryButton}"
                    Width="160" Height="36"
                    HorizontalAlignment="Center"
                    Click="SaveButton_Click"/>

            <Button x:Name="CancelButton"
                    Grid.Column="1"
                    Content="Отмена"
                    Style="{StaticResource BaseButton}"
                    Width="140" Height="36"
                    HorizontalAlignment="Center"
                    Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</Window>
